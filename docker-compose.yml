services:
  db:
    image: mysql:8
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME:-toolbox_db}
      MYSQL_USER: ${DB_USER:-toolbox_user}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - data:/var/lib/mysql

  main:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DEBUG: ${DEBUG:-false}
    restart: unless-stopped
    depends_on:
      - db
    environment:
      SECRET_KEY: ${SECRET_KEY}
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: toolbox_db
      DB_USER: toolbox_user
      DB_PASSWORD: ${DB_PASSWORD}
      DEBUG: ${DEBUG:-false}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      GITHUB_APP_ID: ${GITHUB_APP_ID}
      GITHUB_INSTALLATION_ID: ${GITHUB_INSTALLATION_ID}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      INTERNAL_BOT_TOKEN: ${INTERNAL_BOT_TOKEN}
    ports:
      - "8000:8000"
    develop:
      watch:
        - action: sync
          path: ./frontend
          target: /home/toolbox/site/frontend
          ignore:
            - node_modules/
        - action: sync+restart
          path: ./backend/main
          target: /home/toolbox/site/backend/main
        - action: rebuild
          path: backend/main/requirements.txt
        - action: rebuild
          path: ./frontend/package.json
    volumes:
      - photos:/home/toolbox/site/backend/main/data/photos

  download:
    build:
      context: backend/download
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8001:8001"
    develop:
      watch:
        - action: sync+restart
          path: ./backend/download
          target: /home/toolbox/site/backend/download
        - action: rebuild
          path: backend/main/requirements.txt
        - action: rebuild
          path: ./frontend/package.json
    volumes:
      - download_cache:/home/toolbox/site/backend/download/cache

  supportbot:
    build:
      context: backend/supportbot
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - main
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      API_BASE_URL: http://main:8000
      INTERNAL_BOT_TOKEN: ${INTERNAL_BOT_TOKEN}
    develop:
      watch:
        - action: sync+restart
          path: ./backend/supportbot
          target: /root/site/backend/supportbot

  caddy:
    build:
      context: ./caddy
      dockerfile: Dockerfile
    restart: unless-stopped
    profiles: ["prod"]
    ports:
      - "80:80"
      - "443:443"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - certs:/root/site/certs
    environment:
      XDG_DATA_HOME: /root/site/certs
      XDG_CONFIG_HOME: /root/site/certs

volumes:
  data:
    name: toolbox-io-data
  certs:
  download_cache:
  photos:
  huggingface_cache: