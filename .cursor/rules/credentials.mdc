---
alwaysApply: true
---

When working with API authentication, do NOT use { credentials: "include" }, 
instead use { 
    headers: {
         "Authorization": `Bearer ${localStorage.getItem("authToken")}`
    } 
}.